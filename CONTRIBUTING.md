# コントリビューションガイド

改善提案、新しいスキル、検証の強化を歓迎します。変更は、公開しても安全で、特定の個人や環境に依存しない内容にしてください。

## スキルの追加手順

1. `templates/skill-template/` を `skills/<skill-name>/` へコピーします。
2. フォルダ名と `SKILL.md` の `name` を一致させます。
3. `description` に、スキルが行うことと使用する場面を書きます。
4. 本文を500行以内にまとめます。長い資料は `references/` へ分けます。
5. 実際の依頼を使い、スキルなしの場合と使用した場合を比較します。
6. ローカル検証を実行し、pull requestを作成します。

## ローカル検証

Python 3.10以降を使用します。

```sh
python scripts/validate_skills.py skills templates/skill-template
python -m unittest discover -s tests -v
```

## pull requestの確認項目

- [ ] 1つのスキルが1つの仕事に集中している
- [ ] `name` とフォルダ名が一致している
- [ ] `description` から用途と使用場面が分かる
- [ ] 入力にない事実や環境固有値を作る指示がない
- [ ] 個人情報、認証情報、内部ホスト名、IPアドレス、個人用パスを含まない
- [ ] 破壊的操作や外部変更を、確認なしで実行させない
- [ ] 実際の利用例で動作を確認した
- [ ] 検証スクリプトとテストが成功した

## 文章と例

- 日本語は、短い文と一般的なIT用語を使います。
- 例には `{host}`、`example.invalid`、予約済みのドキュメント用IPアドレスなどを使います。
- 実在する非公開環境の文章を、値だけ置き換えて転載しません。
- 時期によって変わる製品仕様には、公式資料へのリンクと確認日を付けます。

コントリビューションは、このリポジトリのMIT Licenseで公開されることに同意したものとして扱います。
